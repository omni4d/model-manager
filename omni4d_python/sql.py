import tqdm
import logging
from omni4d_python import object_types, role_types

logger = logging.getLogger(__name__)


def create_db(db_name, server, port, user, password):
    logger.debug('sql.create_db:')


def import_model(model, db_name, server, port, user, password):
    logger.debug('sql.import_model:')
